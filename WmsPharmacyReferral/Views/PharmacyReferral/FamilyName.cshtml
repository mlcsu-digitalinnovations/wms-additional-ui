@model WmsReferral.Business.Models.FamilyNameViewModel
@using WmsReferral.Business.Helpers

@{
    ViewData["Title"] = "What is the patient's name?";
    Layout = "~/Views/Shared/_Layout.cshtml";
    bool hasErrors = ViewData.ModelState.ErrorCount > 0;
}

<div class="nhsuk-grid-row">
    <div class="nhsuk-grid-column-full">

        <div class="nhsuk-back-link">
            <a class="nhsuk-back-link__link" asp-action="date-of-birth">
                <svg class="nhsuk-icon nhsuk-icon__chevron-left" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" aria-hidden="true">
                    <path d="M8.5 12c0-.3.1-.5.3-.7l5-5c.4-.4 1-.4 1.4 0s.4 1 0 1.4L10.9 12l4.3 4.3c.4.4.4 1 0 1.4s-1 .4-1.4 0l-5-5c-.2-.2-.3-.4-.3-.7z"></path>
                </svg>
                Go back
            </a>
        </div>

        <div class="nhsuk-card wms-card">
            <div class="nhsuk-card__content">
                <h1 class="nhsuk-heading-l">Section 2: Patient personal information</h1>
                <form asp-action="family-name"
                      method="post">
                    <div class="@NhsFormHelper.GetFormElementClasses("form-group", hasErrors)">
                        <fieldset class="nhsuk-fieldset nhsuk-u-padding-bottom-6"
                                  aria-describedby="FamilyName-hint">                            
                            <h2 class="nhsuk-heading-m">
                                <label class="nhsuk-label nhsuk-label--m">
                                    What is the patient's last name?
                                </label>
                            </h2>
                            <div class="nhsuk-hint" id="FamilyName-hint">
                                Last name is sometimes referred to as surname or family name
                            </div>
                            <span class="nhsuk-error-message" id="input-with-error-message-error">
                                <span asp-validation-for="FamilyName">Enter the patient's last name to continue with the application</span>
                            </span>
                            <input type="text" class="@NhsFormHelper.GetFormElementClasses("text", hasErrors)" asp-for="FamilyName" autocomplete="off" spellcheck="false" />
                        </fieldset>
                        <input type="submit" value="Continue" class="nhsuk-button" />
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
@section scripts{
    <partial name="_ValidationScriptsPartial" />
}